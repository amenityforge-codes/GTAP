/** Production site URL — set this to your real domain (e.g. .com or your host’s default URL). */
export const SITE_URL = "https://globaltimespanel.com" as const;

export const SITE_NAME = "GTAP - Global T.I.M.E.S Accreditation Panel";
